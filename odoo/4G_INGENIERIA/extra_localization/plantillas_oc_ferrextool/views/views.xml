<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="plantillas_facturas_ferrextool.list">
      <field name="name">plantillas_facturas_ferrextool list</field>
      <field name="model">plantillas_facturas_ferrextool.plantillas_facturas_ferrextool</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="plantillas_facturas_ferrextool.action_window">
      <field name="name">plantillas_facturas_ferrextool window</field>
      <field name="res_model">plantillas_facturas_ferrextool.plantillas_facturas_ferrextool</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="plantillas_facturas_ferrextool.action_server">
      <field name="name">plantillas_facturas_ferrextool server</field>
      <field name="model_id" ref="model_plantillas_facturas_ferrextool_plantillas_facturas_ferrextool"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="plantillas_facturas_ferrextool" id="plantillas_facturas_ferrextool.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="plantillas_facturas_ferrextool.menu_1" parent="plantillas_facturas_ferrextool.menu_root"/>
    <menuitem name="Menu 2" id="plantillas_facturas_ferrextool.menu_2" parent="plantillas_facturas_ferrextool.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="plantillas_facturas_ferrextool.menu_1_list" parent="plantillas_facturas_ferrextool.menu_1"
              action="plantillas_facturas_ferrextool.action_window"/>
    <menuitem name="Server to list" id="plantillas_facturas_ferrextool" parent="plantillas_facturas_ferrextool.menu_2"
              action="plantillas_facturas_ferrextool.action_server"/>
    -->
  </data>
</odoo>